# Firebase App Hosting configuration
# This file configures how your app is built and deployed

# Build configuration
build:
  # Use Node.js 18 runtime
  runtime: nodejs18
  
  # Build command (if needed)
  # buildCommand: npm run build
  
  # Entry point for the application
  entrypoint: node index.js

# Environment variables
env:
  # Razorpay configuration
  RAZORPAY_KEY_ID: rzp_live_RBwW40KxX2ChHW
  RAZORPAY_KEY_SECRET: "your_razorpay_secret_here"
  
  # JWT secret for authentication
  JWT_SECRET: vibe_coding_jwt_secret_2024
  
  # MongoDB connection string
  MONGODB_URI: "mongodb+srv://vibecoding:vibecoding@vbcluster0.1c8ero3.mongodb.net/vibe-coding?retryWrites=true&w=majority&appName=vbCluster0"
  
  # Server configuration
  PORT: 8080
  
  # LinkedIn OAuth configuration
  LINKEDIN_CLIENT_ID: "your_linkedin_client_id"
  LINKEDIN_CLIENT_SECRET: "your_linkedin_client_secret"
  LINKEDIN_REDIRECT_URI: "https://vibe-coding--library-management-806d9.asia-east1.hosted.app/linkedin-callback"
  
  # Frontend URL for redirects
  FRONTEND_URL: "https://vibe-coding.lifestyle"

# Health check configuration
healthCheck:
  # Path for health check endpoint
  path: /health
  # Timeout in seconds
  timeout: 30

# Resource allocation
resources:
  # CPU allocation (0.5 to 4.0)
  cpu: 1.0
  # Memory allocation (512MB to 8GB)
  memory: 1Gi

# Scaling configuration
scaling:
  # Minimum number of instances
  minInstances: 1
  # Maximum number of instances
  maxInstances: 10
  # Target CPU utilization for auto-scaling
  targetCpuUtilization: 60

# Network configuration
network:
  # Allow all traffic (adjust as needed)
  ingress: all

# Logging configuration
logging:
  # Log level (debug, info, warn, error)
  level: info
